% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_stats.R
\name{cluster_inter_distances}
\alias{cluster_inter_distances}
\title{Compute per-cluster nearest-neighbor SNP distances}
\usage{
cluster_inter_distances(clusters, snp_dist)
}
\arguments{
\item{clusters}{A vector named by sequence IDs giving the cluster each sequence belongs to.}

\item{snp_dist}{A matrix of SNP distances between isolates. Its row/column names define the full
universe of isolates, including those not assigned to any cluster.}
}
\value{
A matrix with one row per cluster (named by cluster ID) and columns \code{min_inter_cluster}
and \code{min_inter_isolate}.
}
\description{
For every non-singleton cluster, the minimum SNP distance to an isolate in another cluster
(\code{min_inter_cluster}) and to any isolate outside the cluster (\code{min_inter_isolate}). Unlike
\code{\link[=cluster_pairwise_distances]{cluster_pairwise_distances()}}, this works over the whole clustering at once: it derives each
cluster's "other cluster" and "non cluster" comparison sets from the full assignment vector.
Singleton clusters are dropped via \code{\link[=remove_singleton_clusters]{remove_singleton_clusters()}}, so their sequences never form
a cluster of their own but still count as unclustered isolates toward \code{min_inter_isolate}.
}
