% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_stats.R
\name{assign_study_half}
\alias{assign_study_half}
\title{Assign clusters to halves of the study window}
\usage{
assign_study_half(
  cluster_start_date,
  study_window,
  labels = c("First half", "Second half")
)
}
\arguments{
\item{cluster_start_date}{Numeric vector of cluster start dates, in the same
day-offset units as \code{study_window} (e.g. the \code{cluster_start_date} column of
\code{\link[=compute_duration_metrics_by_cluster]{compute_duration_metrics_by_cluster()}}).}

\item{study_window}{Numeric vector of study dates whose range defines the
study window (e.g. \code{surv_df$surv_date}). Only its min and max are used; the
split point is their midpoint.}

\item{labels}{Length-2 character vector naming the first- and second-half
groups, in that order. Defaults to \code{c("First half", "Second half")}.}
}
\value{
A factor the same length as \code{cluster_start_date}, whose two ordered
levels are \code{labels}.
}
\description{
Classify each transmission cluster as belonging to the first or second half
of the study, based on when the cluster started relative to the midpoint of
the study window. A cluster's start is its \code{cluster_start_date} (the earliest
positive date among the cluster's patients, as returned by
\code{\link[=compute_duration_metrics_by_cluster]{compute_duration_metrics_by_cluster()}}); the study window is the calendar
span over which surveillance was carried out (e.g. the range of
\code{surv_df$surv_date}). Clusters whose start falls on or before the window
midpoint are assigned to the first half, the rest to the second half.

This is the temporal counterpart to binning clusters by their \emph{duration}
(\code{time_to_last_acquisition}): instead of asking how long a cluster stayed
active, it asks \emph{when} in the study the cluster appeared.
}
