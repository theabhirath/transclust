% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_utils.R
\name{enforce_monophyly}
\alias{enforce_monophyly}
\title{Force cluster assignments to be monophyletic with respect to a tree.}
\usage{
enforce_monophyly(
  clusters,
  tree,
  monophyly_method = c("expand", "break_down"),
  special_val = NULL
)
}
\arguments{
\item{clusters}{A named numeric vector of cluster assignments; the names are the isolate/tip
labels used to locate members on the tree.}

\item{tree}{A phylogenetic tree of class \code{phylo}.}

\item{monophyly_method}{How to make a non-monophyletic cluster monophyletic. \code{"expand"} grows the
cluster to the smallest clade of the tree that contains all of its members,
absorbing any intervening isolates. \code{"break_down"} instead splits the cluster into
the largest monophyletic clades it already contains, without pulling in foreign
isolates.}

\item{special_val}{An optional cluster value to leave untouched, e.g. \code{0} for unclustered isolates
that should not be treated as a single cluster. Default is \code{NULL}.}
}
\value{
A numeric vector of cluster assignments that are monophyletic with respect to the tree,
renumbered sequentially.
}
\description{
Checks each multi-isolate cluster for monophyly on \code{tree} and reconciles those that are not
monophyletic using the chosen strategy. The result is renumbered sequentially with
\code{\link[=remap_cluster_values]{remap_cluster_values()}}, and each reconciled cluster is reported via \code{\link[=message]{message()}} against its
final (remapped) cluster id, listing the isolates that make it up.
}
\keyword{internal}
