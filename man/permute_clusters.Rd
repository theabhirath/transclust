% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_stats.R
\name{permute_clusters}
\alias{permute_clusters}
\title{Permute Clusters While Preserving Size Distribution and Patient Counts}
\usage{
permute_clusters(clusters, seq2pt, ip_seqs, ip_pt_seqs, all = FALSE)
}
\arguments{
\item{clusters}{A named numeric vector where names are sequence IDs and values are subtrees defining the cluster.}

\item{seq2pt}{A named vector mapping sequence IDs to patient IDs.}

\item{ip_seqs}{A vector of sequence IDs which correspond to intake patient sequences presumed to be imported.}

\item{ip_pt_seqs}{A vector of sequence IDs which correspond to intake positive patients.}

\item{all}{Logical flag indicating whether patients not included in clusters (i.e. default cluster 1) should
be considered.}
}
\value{
A numeric vector of permuted clusters (with the same names as the input clusters) maintaining the
original size distribution and patient structure.
}
\description{
Randomly assigns sequences to new clusters while maintaining the original distribution of patient types
(index start, index not start, convert) within each cluster size group.
}
\details{
The function categorizes patients within each original cluster into three types: \verb{index start}, \verb{index not start},
and \code{convert}.

The permutation process works by:
\itemize{
\item Calculating the number of unique patients of each category required for each target cluster
(based on the original cluster compositions).
\item Identifying unique "patient-cluster groups" (all sequences from a patient within one original cluster).
\item Prioritizing patients who belong to multiple original clusters to ensure they can be placed correctly.
\item Iteratively assigning each patient-cluster group (belonging to a specific category) to a randomly
chosen eligible target cluster (one that still needs a patient of that category).
}
}
